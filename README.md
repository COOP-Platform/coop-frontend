# COOP — Frontend

Web frontend for COOP, piloted with **Les Cousins Neretse**.

## Stack

| Concern | Choice |
| --- | --- |
| UI | React 18 + TypeScript (strict) |
| Build | Vite 6 |
| Routing | TanStack Router (file-based, type-safe) |
| Server state | TanStack Query |
| Styling | Plain CSS + CSS custom properties (design tokens) |
| Quality | ESLint (flat config) + Prettier |

## Getting started

```bash
npm install
cp .env.example .env.local   # adjust VITE_API_BASE_URL to point at the backend
npm run dev                  # http://localhost:5173
```

You should land on the welcome screen. That's the "starter code runs locally
for at least one dev other than the author" item in the Sprint 0 definition of done.

## Scripts

| Command | Does |
| --- | --- |
| `npm run dev` | Dev server with HMR |
| `npm run build` | Type-check then production build to `dist/` |
| `npm run preview` | Serve the production build locally |
| `npm run typecheck` | Type-check only |
| `npm run lint` / `lint:fix` | ESLint |
| `npm run format` / `format:check` | Prettier |

## Project structure

```
src/
├── main.tsx              # entry point — creates the router, mounts React
├── routeTree.gen.ts      # GENERATED by the TanStack Router plugin — never edit
├── routes/               # file-based routes; the file path IS the URL
│   ├── __root.tsx        #   providers + app shell + 404 / error boundaries
│   └── index.tsx         #   "/" — the welcome screen
├── features/             # one folder per business module (see features/README.md)
│   ├── communities/      #   Sprint 1
│   └── members/          #   Sprint 1
├── components/
│   ├── layout/           # app shell: header, nav, footer
│   └── ui/               # reusable presentational primitives (Figma components)
├── lib/
│   ├── api/client.ts     # typed fetch wrapper + ApiError
│   ├── query-client.ts   # TanStack Query defaults (stale time, retry policy)
│   └── query-keys.ts     # central cache-key registry
├── hooks/                # cross-cutting React hooks
├── types/                # shared domain types (mirrors the ERD)
├── config/env.ts         # validated environment variables
└── styles/
    ├── tokens.css        # >>> COLOURS / TYPE LIVE HERE <<<
    └── global.css        # resets + shared layout classes
```

### Adding a route

Create a file under `src/routes/`. The plugin regenerates `routeTree.gen.ts`
automatically while `npm run dev` is running.

```
src/routes/communities/index.tsx        →  /communities
src/routes/communities/$communityId.tsx →  /communities/:communityId
```

```tsx
export const Route = createFileRoute('/communities/')({
  component: CommunitiesPage,
});
```

### Adding data fetching

Put the call in the feature's `api/` folder, key it from `src/lib/query-keys.ts`:

```ts
export function useCommunities() {
  return useQuery({
    queryKey: queryKeys.communities.list(),
    queryFn: () => apiFetch<Paginated<Community>>('/communities'),
  });
}
```

## Colours and branding — read before styling anything

The palette is **not decided yet**. `src/styles/tokens.css` holds neutral
greyscale placeholders so nothing needs unpicking later.

- Never hardcode a colour in a component. Use `var(--color-…)`.
- When the Figma palette is approved, change **only** the values in
  `tokens.css` and the whole app updates.

## Sprint 1 scope

20 Aug – 02 Sep 2026 — *"Communities and members exist in the system."*

- Community Management
- Member Management (invite / approve / category flow)

Auth and RBAC arrive in Sprint 2, so the API client currently sends no
`Authorization` header — that hook-in point is marked in `src/lib/api/client.ts`.

## Deployments

`develop` is deployed by **GitHub Actions only** — see
`.github/workflows/deploy-develop.yml`. Every push to `develop` builds and
publishes to **Production** via the Vercel CLI
(`vercel pull --environment=production` → `vercel build --prod` →
`vercel deploy --prebuilt --prod`).

So the project's main Vercel URL always reflects the newest `develop`. There is
no separate release step, which is the trade-off: whatever merges to `develop`
is immediately live, and CI is the only gate in front of it.

The three CLI calls must agree on the target. `vercel pull` decides which
environment's `VITE_*` values are inlined at build time, so pulling `preview`
and deploying `--prod` would ship a production URL built with preview
configuration.

### Why `vercel.json` disables Git deployments for `develop`

```json
{ "git": { "deploymentEnabled": { "develop": false } } }
```

This stays **off** even though `develop` now goes to production, and the reason
is unchanged: Vercel's Git integration would deploy the same commit in parallel
with the Actions pipeline, so every push was built twice and two deployments
raced for the production alias. Vercel defaults its production branch to the
repo default branch, which here is `develop`, so it needs to be disabled
explicitly.

`deploymentEnabled` governs only the Git integration — CLI deploys are
unaffected, which is why the workflow's `--prod` deploy still works. GitHub
Actions is the single deployer.

If you ever want Vercel's automatic PR previews back, remove that key, but
expect the double-deploy behaviour to return for `develop`.

### Why `vercel.json` rewrites everything to `/index.html`

```json
{ "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }] }
```

This is a client-routed SPA: only `/index.html` exists on disk, and TanStack
Router resolves the path in the browser. Without the rewrite, Vercel answers
any deep link with a hard 404 — `/login`, `/register` and every
`/invitation/*` and `/onboarding/*` URL was unreachable except by navigating
from `/`, which matters because invitation links are emailed as deep URLs.

Vercel's Vite preset does not add this fallback on its own; it was verified
missing against the live deployment. `rewrites` are evaluated *after* the
filesystem check, so hashed assets under `/assets/` still serve normally and
only unmatched paths fall through to the app.

Required secrets: `VERCEL_TOKEN`, `VERCEL_ORG_ID`, `VERCEL_PROJECT_ID`.
The token must be scoped to the **team** that owns the project, not a personal
account.

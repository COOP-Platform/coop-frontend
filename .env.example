# Base URL of the backend API (Jean Claude's service).
# Copy this file to .env.local and adjust as needed.
VITE_API_BASE_URL=http://localhost:8080/api

# Human-readable app name shown in the UI.
VITE_APP_NAME=COOP
